@model Bulgarikon.Core.DTOs.CivilizaionDTOs.CivilizationViewDto

@{
    ViewData["Title"] = "Детайли";
    bool isAdmin = User.IsInRole("Admin");

    var duration = Model.EndYear - Model.StartYear;
    var durationText = duration >= 0 ? $"{duration} г." : "—";
    var hasImage = !string.IsNullOrWhiteSpace(Model.ImageUrl);
}

<div class="era-shell">
    <div class="era-header">
        <div>
            <div class="era-kicker">Bulgarikon • Цивилизация</div>
            <h1 class="era-h1">@Model.Name</h1>

            <div class="era-sub">
                <span class="era-chip">@Model.StartYear – @Model.EndYear</span>
                <span class="era-chip">@Model.Type</span>

                @if (!string.IsNullOrWhiteSpace(Model.EraName))
                {
                    <span class="era-chip">Епоха: @Model.EraName</span>
                }
            </div>
        </div>

        <div class="era-actions">
            <a asp-action="Index" asp-route-eraId="@Model.EraId" class="btn btn-outline-secondary">← Назад</a>

            @if (isAdmin)
            {
                <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-outline-primary">Редактирай</a>

                <button type="button"
                        class="btn btn-outline-danger"
                        data-bk-delete-civ
                        data-id="@Model.Id"
                        data-name="@Model.Name"
                        data-start="@Model.StartYear"
                        data-end="@Model.EndYear"
                        data-eraid="@Model.EraId">
                    Изтрий
                </button>
            }
        </div>
    </div>

    <div class="card shadow-sm era-toolbar mt-3">
        <div class="card-body" style="display:grid; grid-template-columns: repeat(4, minmax(0, 1fr)); gap:12px;">
            <div class="era-count-box">
                <div class="era-count-num">@Model.StartYear</div>
                <div class="era-count-label">Начало</div>
            </div>

            <div class="era-count-box">
                <div class="era-count-num">@Model.EndYear</div>
                <div class="era-count-label">Край</div>
            </div>

            <div class="era-count-box">
                <div class="era-count-num">@durationText</div>
                <div class="era-count-label">Продължителност</div>
            </div>

            <div class="era-count-box">
                <div class="era-count-num">@(hasImage ? "1" : "0")</div>
                <div class="era-count-label">Снимки</div>
            </div>
        </div>
    </div>

    <div class="card shadow-sm era-paper mt-3">
        <div class="card-body">
            <h5 class="mb-2">Снимка</h5>

            @if (!hasImage)
            {
                <div class="text-muted small">Няма снимка.</div>
            }
            else
            {
                <img src="@Model.ImageUrl"
                     alt="@Model.Name"
                     style="width:100%; max-height:340px; object-fit:cover; border-radius:16px;" />
            }
        </div>
    </div>

    <div class="card shadow-sm era-paper mt-3">
        <div class="card-body">
            <div class="d-flex align-items-center justify-content-between gap-2 flex-wrap">
                <h5 class="mb-0">Описание</h5>
                <button type="button" class="btn btn-sm btn-outline-secondary" id="bkToggleHighlights">Подчертай години</button>
            </div>

            @if (string.IsNullOrWhiteSpace(Model.Description))
            {
                <div class="alert alert-info mt-3 mb-0">Няма описание.</div>
            }
            else
            {
                <div class="mt-3 era-readable" id="bkDesc">@Model.Description</div>
            }
        </div>
    </div>

    @await Html.PartialAsync("Partials/_DeleteCivilizationModal")
</div>

@section Scripts {
    <script>
        (function () {
            const descEl = document.getElementById('bkDesc');
            const toggleBtn = document.getElementById('bkToggleHighlights');

            if (descEl && toggleBtn) {
                let highlighted = false;
                const originalHtml = descEl.innerHTML;

                toggleBtn.addEventListener('click', () => {
                    if (highlighted) {
                        descEl.innerHTML = originalHtml;
                        highlighted = false;
                        toggleBtn.textContent = 'Подчертай години';
                        return;
                    }

                    descEl.innerHTML = originalHtml.replace(
                        /\b(1[0-9]{3}|20[0-9]{2})\b/g,
                        `<mark style="padding:0 4px; border-radius:6px;">$1</mark>`
                    );

                    highlighted = true;
                    toggleBtn.textContent = 'Махни подчертаване';
                });
            }
        })();
    </script>
}